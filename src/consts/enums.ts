export enum VaultStatus {
  Valid = 'Valid',
  Removed = 'Removed',
  Protected = 'Protected',
}
export enum ArchiveStatus {
  Archived = 'Archived',
  Restored = 'Restored',
}

export enum CheckIssueLevel {
  Info = 'Info',
  Warn = 'Warn',
  Error = 'Error',
}
